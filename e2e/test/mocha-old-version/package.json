{
  "name": "mocha-old-version",
  "version": "0.0.0",
  "private": true,
  "description": "A test package for mocha 5",
  "main": "index.js",
  "scripts": {
    "clean:reports": "rimraf \"reports\"",
    "verify": "mocha --no-config --no-package --timeout 0 verify/verify.js",
    "test:unit": "mocha",
    "test": "npm run clean:reports && stryker run && npm run verify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "7.2",
    "@ryicoh/core": "workspace:*",
    "@ryicoh/mocha-runner": "workspace:*"
  },
  "dependenciesMeta": {
    "@ryicoh/core": {
      "injected": true
    },
    "@ryicoh/mocha-runner": {
      "injected": true
    }
  }
}
